import { ErrorBoundary } from '@shared';

const DashboardPage = () => {
  return (
    <ErrorBoundary>
      <p>This is Dashboard page.</p>
    </ErrorBoundary>
  );
};

export default DashboardPage;
